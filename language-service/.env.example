# Optional: enables real Claude feedback. Without it the service runs in offline mode.
ANTHROPIC_API_KEY=
# Claude model id (Sonnet recommended for tutoring; Opus for max quality).
LLM_MODEL=claude-sonnet-4-6
